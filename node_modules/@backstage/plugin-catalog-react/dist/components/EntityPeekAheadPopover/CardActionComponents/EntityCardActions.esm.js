import { entityRouteRef } from '../../../routes.esm.js';
import IconButton from '@material-ui/core/IconButton';
import InfoIcon from '@material-ui/icons/Info';
import React from 'react';
import { useRouteRef } from '@backstage/core-plugin-api';
import { getCompoundEntityRef } from '@backstage/catalog-model';
import { Link } from '@backstage/core-components';

const EntityCardActions = (props) => {
  const entityRoute = useRouteRef(entityRouteRef);
  return /* @__PURE__ */ React.createElement(
    IconButton,
    {
      component: Link,
      "aria-label": "Show",
      title: "Show details",
      to: entityRoute(getCompoundEntityRef(props.entity))
    },
    /* @__PURE__ */ React.createElement(InfoIcon, null)
  );
};

export { EntityCardActions };
//# sourceMappingURL=EntityCardActions.esm.js.map
